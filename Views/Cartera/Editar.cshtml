@model ReclamosWhatsApp.Models.Cliente

@{
    ViewData["Title"] = "Editar cliente";
    var telefonos = (IEnumerable<string>)ViewBag.Telefonos;
}

<div class="container mt-4">
    <h2>Editar cliente</h2>

    <form method="post" class="card shadow-sm border-0 p-4">
        @Html.AntiForgeryToken()

        <input type="hidden" asp-for="Id" />

        <div class="row">
            <div class="col-md-6 mb-3">
                <label>Nombre</label>
                <input asp-for="Nombre" class="form-control" required />
            </div>

            <div class="col-md-3 mb-3">
                <label>Teléfono principal</label>
                <input asp-for="Telefono" class="form-control" />
            </div>

            <div class="col-md-3 mb-3">
                <label>Contacto / otros teléfonos</label>
                <input asp-for="Contacto" class="form-control" />
            </div>

            <div class="col-md-6 mb-3">
                <label>Correo</label>
                <input asp-for="Email" class="form-control" />
            </div>

            <div class="col-md-3 mb-3">
                <label>Identidad</label>
                <input asp-for="Identidad" class="form-control" />
            </div>

            <div class="col-md-3 mb-3">
                <label>Cumpleaños</label>
                <input asp-for="FechaNacimiento" type="date" class="form-control" />
            </div>

            <div class="col-md-4 mb-3">
                <label>Ciudad</label>
                <input asp-for="Ciudad" class="form-control" />
            </div>

            <div class="col-md-8 mb-3">
                <label>Observaciones</label>
                <textarea asp-for="Observaciones" class="form-control"></textarea>
            </div>

            <div class="col-md-12 mb-3">
                <label>
                    <input asp-for="Activo" />
                    Activo
                </label>
            </div>
        </div>

        <div class="mb-3">
            <h5>Teléfonos registrados</h5>
            @if (telefonos.Any())
            {
                foreach (var tel in telefonos)
                {
                    <span class="badge bg-primary me-1">@tel</span>
                }
            }
            else
            {
                <span class="text-muted">Sin teléfonos adicionales.</span>
            }
        </div>

        <div class="d-flex gap-2">
            <button class="btn btn-primary">Guardar cambios</button>
            <a href="/Cartera" class="btn btn-secondary">Cancelar</a>
        </div>
    </form>
</div>
